interface:
  display_name: "J-Link MCP"
  short_description: "Operate and inspect one local J-Link target"
  default_prompt: "Use $jlink-mcp to show the current target status without changing hardware state."
dependencies:
  tools:
    - type: "mcp"
      value: "jlink_mcp"
      description: "Local J-Link MCP V1 server"
policy:
  allow_implicit_invocation: true
