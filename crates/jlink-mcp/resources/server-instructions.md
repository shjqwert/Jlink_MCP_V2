J-Link MCP V1 exposes exactly six tools. Live `tools/list` schemas are authoritative; send only declared fields. Successful `structuredContent` is authoritative; `{}` means success. On `EXECUTION_UNCERTAIN`, do not repeat the side effect; rebuild trusted state, verify safely, or ask the user.
