J-Link MCP V1 exposes exactly six tools. Only one named logical agent may operate the target at a time, including reads. Transfer ownership only after the previous operator's calls have finished. Live `tools/list` schemas are authoritative; send only declared fields. Successful `structuredContent` is authoritative; `{}` means success. On `EXECUTION_UNCERTAIN`, do not repeat the side effect; rebuild trusted state, verify safely, or ask the user.
