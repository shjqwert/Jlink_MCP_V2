J-Link MCP V1 exposes exactly six tools. One named operator owns all calls, including reads; active capture/control stays with it until terminal state or explicit handoff. Live `tools/list` schemas and `structuredContent` are authoritative. On `EXECUTION_UNCERTAIN`, do not repeat the side effect.
