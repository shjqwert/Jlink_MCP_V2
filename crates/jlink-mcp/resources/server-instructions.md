J-Link MCP V1 exposes exactly six tools for one local target and immutable captures. Treat each live `tools/list` input and output Schema as the syntax authority, and send only declared fields. Successful `structuredContent` is authoritative; an empty object is a completed success. If a program, write, or control result is `EXECUTION_UNCERTAIN`, do not repeat the side effect; re-establish trustworthy session state and verify safely or ask the user.
